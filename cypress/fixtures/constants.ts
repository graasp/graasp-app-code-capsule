export const SLOW_DOWN_CYPRESS_DELAY = 1000;
export const REPL_TIMEOUT = 180000; // max timeout after which the repl errors
export const SLOW_TYPING_IN_EDITOR = 100; // type slower than the default (10) to not miss characters
